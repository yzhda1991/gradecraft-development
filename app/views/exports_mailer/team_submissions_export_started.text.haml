Dear #{ @professor.first_name },

Your requested submissions export of #{ @course.assignment_term.downcase} "#{ @assignment.name }" in course #{ @course.name } for #{ @course.team_term.downcase } #{ @team.name } is being created.

You'll be notified when your ZIP archive is ready to download.

Sincerely,
GradeCraft
