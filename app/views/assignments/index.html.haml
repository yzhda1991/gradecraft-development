- if current_user_is_staff?
  = render partial: "assignments/index_staff"
- elsif current_user_is_student?
  = render partial: "assignments/index_student", locals: { presenter: presenter }
- else
  = render partial: "assignments/index_observer", locals: { presenter: presenter }
