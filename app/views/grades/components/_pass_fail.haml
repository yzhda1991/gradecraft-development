- if GradeProctor.new(presenter.grade_for(assignment)).viewable?
  - if presenter.grade_for(assignment).pass_fail_status
    %p= term_for(presenter.grade_for(assignment).pass_fail_status)
- else
  %p= "#{term_for :pass}/#{term_for :fail}"
