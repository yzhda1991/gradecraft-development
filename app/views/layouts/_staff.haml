.sidebar
  / Staff-only sidebar navigation
  = render 'layouts/navigation/staff_subnav'

.mainContainer.staff{:role => "main"}

  - if current_student.present?
    = content_nav do
      = render "students/student_profile_tabs"

  / Page content
  .panel.mainContent{ :class => (current_student ? 'student' : '') }= render 'layouts/content'

  - if current_student.present?
    .student-sidebar
      = render "students/student_sidebar"