%p
  %span.smallcaps Attachments:
  %ul
    - presenter.submission.submission_files.each_with_index do |submission_file, index|
      %li
        = external_link_to submission_file.instructor_filename(index),
          download_submission_file_path(submission_file)
