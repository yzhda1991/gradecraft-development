- if (presenter.assignment.open? && !presenter.submission.grade.present?) || (presenter.assignment.open? && presenter.assignment.resubmissions_allowed?)

  = link_to "Edit My Submission", edit_assignment_submission_path(presenter.assignment, presenter.submission), class: "button"