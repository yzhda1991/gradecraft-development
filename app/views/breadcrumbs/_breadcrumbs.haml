%ul.breadcrumbs
  - breadcrumbs.each do |breadcrumb|
    - if breadcrumb.link?
      %li= link_to instance_eval(%{ "#{ breadcrumb.label }" }), breadcrumb.url
    - else
      %li= instance_eval(%{ "#{ breadcrumb.label }" })
