= content_nav_for @team, "Edit #{term_for :team}"

%h3.pagetitle= @title

.context_menu
  %ul
    %li
      %a{:href => team_path(@team) }
        %i.fa.fa-list
        See #{term_for :team}

.pageContent
  = render 'form'