%section.grade-status
  .grade-section-label Grade Status

  %select.grade-status-selector(ng-model='grade.status')
    %option(value='{{statusOption}}' ng-repeat='statusOption in statusOptions()' ng-selected='grade.status == statusOption')
      {{statusOption}}
