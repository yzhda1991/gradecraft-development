%section.form-section
  %h2.form-title {{termFor("assignment")}} Dates
  .form-subsection
    %assignment-date-picker(assignment="assignment")

  %h2.form-title {{termFor("assignment")}} Grading And Submission
  .form-subsection
    %h3.form-subtitle Grading Style
    .form-flex-row
      .form-item
        %label
          %input(type="checkbox" ng-model="assignment.pass_fail" ng-click="updateAssignment()") Pass Fail
    .form-flex-row(ng-if="!assignment.pass_fail")
      .form-item
        %label
          Total Points Possible
          %input(ng-model="assignment.full_points" gc-number-input ng-change="updateAssignment()")
      .form-item
        %label
          Points Threshold
          %input(ng-model="assignment.threshold_points" gc-number-input ng-change="updateAssignment()")

  .form-subsection(ng-if="!assignment.pass_fail")
    %assignment-score-levels(assignment="assignment")

  %h2.form-title Grading Settings
  .form-subsection
    .form-item
      %label
        %input(type="checkbox" ng-model="assignment.student_logged" ng-click="updateAssignment()")
          Student Logged
          %span.has-tooltip
            %i.fa.fa-info-circle
            .display-on-hover.hover-style
              Do {{ termFor("students")}} self-report their grade for this {{ termFor("assignment")}}?  If you add grade levels, they'll be able to select their self-assessed score from the list during the open time period. If you don't, they'll be able to select that they did the work and earn full points.

    -# TODO: This is in conflict with the new Grade Release process, remove if that is merged before this branch!
    .form-item
      %label
        %input(type="checkbox" ng-model="assignment.release_necessary" ng-click="updateAssignment()")
          Release Necessary
          %span.has-tooltip
            %i.fa.fa-info-circle
            .display-on-hover.hover-style
              Do you want to release all grades at once? This is particularly useful for situations where extensive feedback is important.

    .form-item
      %label
        %input(type="checkbox" ng-model="assignment.accepts_submissions" ng-click="updateAssignment()")
          Accepts Submissions
          %span.has-tooltip
            %i.fa.fa-info-circle
            .display-on-hover.hover-style
              Will you be using GradeCraft to accept submissions for this {{ termFor("assignment")}}?

      %ul.submit.accepts_submission_types.conditional-options.indented(ng-if="assignment.accepts_submissions")
        %li
          %label
            %input(type="checkbox" ng-model="assignment.accepts_links" ng-click="updateAssignment()") Accepts Links
        %li
          %label
            %input(type="checkbox" ng-model="assignment.accepts_attachments" ng-click="updateAssignment()") Accepts Attachments
        %li
          %label
            %input(type="checkbox" ng-model="assignment.accepts_text" ng-click="updateAssignment()")  Accepts Text

    .form-item(ng-if="assignment.accepts_submissions")
      %label
        %input(type="checkbox" ng-model="assignment.resubmissions_allowed" ng-click="updateAssignment()")
          Resubmissions Allowed
          %span.has-tooltip
            %i.fa.fa-info-circle
            .display-on-hover.hover-style
              Can {{ termFor("students")}} resubmit this {{ termFor("assignment")}}?



  %h2.form-title Assignment Visibility Settings
  .form-subsection
