@gradecraft.filter 'AssociateAssignments', ->
  # (assignmentType, assignments) ->
  #     filteredList = []
  #     angular.forEach(assignments, (assignment) ->
  #       if(assignment.assignment_type_id == assignmentType.id)
  #         filteredList[assignment.position] = assignment
  #     )
  #     return filteredList
