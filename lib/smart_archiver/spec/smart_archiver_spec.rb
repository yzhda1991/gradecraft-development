require 'spec_helper'

describe SmartArchiver do
  it 'has a version number' do
    expect(SmartArchiver::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
