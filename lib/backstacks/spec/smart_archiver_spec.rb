require 'spec_helper'

describe Backstacks do
  it 'has a version number' do
    expect(Backstacks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
